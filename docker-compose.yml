version: "3"
services:
  surrealdb:
    image: surrealdb/surrealdb:latest
    user: root
    ports:
      - "172.17.0.1:27912:8000"
    env_file:
      - ./.env
    command: start
    volumes:
      - ./db:/db
